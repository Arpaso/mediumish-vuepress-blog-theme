---
label: Team
hide_body: false
fields:
- name: portrait
  type: file
  config:
    maxSize: 64
  label: Portrait
  hidden: false
- name: full_name
  type: text
  config:
    required: true
    min: 3
  label: Full Name
- name: description
  type: textarea
  default: ''
  config:
    required: true
    wysiwyg: false
    schema:
      format: markdown
  label: description
